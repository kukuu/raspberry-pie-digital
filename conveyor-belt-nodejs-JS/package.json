{
    "name": "conveyor-belt-simulation",
    "version": "1.0.0",
    "description": "Factory conveyor belt simulation with worker coordination",
    "main": "index.js",
    "scripts": {
      "start": "node index.js",
      "test": "jest --coverage",
      "test:unit": "jest test/unit --coverage",
      "test:integration": "jest test/integration --coverage",
      "test:watch": "jest --watch",
      "metrics": "node ./lib/metrics.js"
    },
    "dependencies": {
      "express": "^4.18.2",
      "onoff": "^6.0.3",
      "prom-client": "^14.2.0",
      "sqlite3": "^5.1.6"
    },
    "devDependencies": {
      "jest": "^29.7.0",
      "supertest": "^6.3.3"
    },
    "jest": {
      "testEnvironment": "node",
      "coveragePathIgnorePatterns": [
        "/node_modules/",
        "/test/"
      ]
    }
  }